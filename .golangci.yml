version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
  disable:
    - errcheck
  settings:
    govet:
      disable:
        - fieldalignment
        - shadow
