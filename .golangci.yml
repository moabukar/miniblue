linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
  disable:
    - errcheck  # HTTP handlers intentionally ignore json.Encode errors on ResponseWriter

linters-settings:
  govet:
    enable-all: true
    disable:
      - fieldalignment

issues:
  exclude-use-default: true
